PROGRAM AREA_DO_TRIANGULO;
USES CRT;
VAR
   AREA,BASE,HALTURA:REAL;
BEGIN
     CLRSCR;
     WRITELN('DIGITE A BASE E A HALTURA DO TRIANGULO');
     READLN(BASE,HALTURA);
     AREA := (BASE * HALTURA)/2;
     WRITELN('AREA DO TRINGULO = ',AREA:0:2);
     REPEAT UNTIL KEYPRESSED
END.