PROGRAM DISCRIMINATE;
USES CRT;
VAR
   A,B,C,D    : REAL;
   CHAVE      : CHAR;
BEGIN
  CLRSCR;
  WINDOW(1,1,78,6);
  TEXTBACKGROUND(13);
  CLRSCR;

  WINDOW(3,2,80,8);
  TEXTBACKGROUND(15);
  TEXTCOLOR(0);
  CLRSCR;
  GOTOXY(8,2);
  WRITE('T R A B A L H O  D E  A N A L I S E  D E  A L G O R I T M O ');
  GOTOXY(18,4);
  WRITE('SOLICITADO PELA PROFESSORA : CRISTINA');
  GOTOXY(10,6);
  WRITE('DIGITE OS VALORES DE A, B E C PARA CALCULO DE DETERMINANTE');
  WINDOW(1,8,80,25);
  TEXTBACKGROUND(0);
  TEXTCOLOR(15);
  CLRSCR;
  CHAVE := 'S';
  WHILE (CHAVE<>'N') AND (CHAVE<>'n') DO
   BEGIN
    CLRSCR;
    WRITELN;
    WRITE('ENTRE COM O PRIMEIRO VALOR: ');
    READLN(A);
    WRITE('ENTRE COM O SEGUNDO  VALOR: ');
    READLN(B);
    WRITE('ENTRE COM O TERCEIRO VALOR: ');
    READLN(C);
    WRITELN;
    D := (B*B-4*A*C);
    IF D>=0 THEN
      BEGIN
         D := SQRT(D);
         WRITE('O VALOR DA RA÷Z ê: ',D:10:2);
       END
    ELSE
       WRITE('NAO EXISTE RA÷ZES');PROGRAM AVALIA;
USES CRT;
CONST
     TOTAL = 5;
TYPE
    GRAU = ARRAY [1..TOTAL] OF REAL;
VAR
   NOTA