PROGRAM CENTRALIZACAO;
USES CRT;
VAR
   FRASE : STRING;
   X     : INTEGER;
   TECLA : CHAR;
BEGIN
     CLRSCR;
     WRITELN ('DIGITE UMA FRASE ');
     READLN (FRASE);
     X := LENGTH(FRASE);
     GOTOXY(40 - X DIV 2,12);
     WRITELN(FRASE);
     TECLA := READKEY;
END.